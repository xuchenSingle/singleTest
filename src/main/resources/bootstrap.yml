spring: 
  application:
    name: cmbs-query-order
    index: ${random.long}
  cloud:
    config:
      name: ms-rainbowStone-cmbs-query-order
      uri: http://${ENV_CONFIG_IP:10.21.20.235}:${CONFIG_PORT:7777}
      profile: ${ENV_TYPE:dev}
      label: master
      failFast: true